// Mengakses nilai input dengan id 'nama'
var nama = document.getElementById('nama').value;

// Mengakses nilai input dengan id 'nim'
var nim = document.getElementById('nim').value;

// Mengakses nilai input dengan id 'email'
var email = document.getElementById('email').value;

// Mengakses nilai input dengan id 'prodi'
var prodi = document.getElementById('prodi').value;

// Mengakses nilai input dengan id 'kelamin'
var kelamin = document.getElementById('kelamin').value;

// Mengakses nilai input dengan id 'tgl'
var tgl = document.getElementById('tgl').value;

// Mengakses nilai input dengan id 'telepon'
var telepon = document.getElementById('telepon').value;

// Mengakses nilai input dengan id 'alamat'
var alamat = document.getElementById('alamat').value;
